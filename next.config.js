/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // output: 'export', // BU SATIR VARSA KESİNLİKLE SİL VEYA YORUMA AL
  // images: { unoptimized: true }, // EĞER export KULLANMIYORSAN BUNA GEREK YOK
}

module.exports = nextConfig
